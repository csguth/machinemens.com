// Tailwind Play CDN config: extend with brand colors used across the site.
tailwind.config = {
  theme: {
    extend: {
      colors: {
        gold: "#c9a24b",
        cream: "#f5efe3"
      }
    }
  }
};