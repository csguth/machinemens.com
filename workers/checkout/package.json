{
  "name": "machinemens-checkout-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Cloudflare Worker: server-side PayPal order create/capture + Printful (POD) draft order creation for /shop/. See workers/checkout/README.md.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
